module.exports = {
  tasks: {
    options: {
      filter: 'include',
      tasks: [
        'build',
        'compile',
        'run-server',
        'run-webdriver',
        'run-tests-debug',
        'run-tests',
        'watch'
      ]
    }
  }
};
